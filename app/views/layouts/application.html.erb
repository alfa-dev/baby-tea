<!DOCTYPE html>
<html>
  <head>
    <title>BabyTea</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  	
  	<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no"/>
  </head>

  <body class="<%= current_user.nil? ? "unlogged" : "logged"  %> <%= "admin" if controller.class.parents.include? Admin %> <%="flash-notice" unless flash[:notice].nil? %> <%= action_name %>">
	  
    <%= render partial: '/flash_notice' if flash[:notice] %> 
  	<%= render partial: '/menu_top' %>

  	<main>
    	<%= yield %>
    </main>
  </body>
</html>
